# ==============================================
# H2O Allegiant Backend - Development Environment
# ==============================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - only commit .env.example

# ==============================================
# 🔐 SECRETS (Required - Change these!)
# ==============================================
OPENAI_API_KEY=sk-your-openai-api-key-here
SECRET_KEY=your-secret-key-min-32-chars-generate-with-openssl-rand-hex-32

# ==============================================
# Application
# ==============================================
ENVIRONMENT=development
DEBUG=true
APP_NAME=H2O Allegiant API
APP_VERSION=1.0.0

# ==============================================
# Database - PostgreSQL (Docker)
# ==============================================
POSTGRES_USER=h2o_user
POSTGRES_PASSWORD=h2o_dev_password
POSTGRES_SERVER=postgres
POSTGRES_PORT=5432
POSTGRES_DB=h2o_allegiant

# ==============================================
# Redis Cache (Docker)
# ==============================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# ==============================================
# API Configuration
# ==============================================
API_V1_PREFIX=/api/v1
BACKEND_URL=http://localhost:8000
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ==============================================
# Storage (Local for development)
# ==============================================
USE_LOCAL_STORAGE=true
LOCAL_STORAGE_PATH=./storage

# ==============================================
# OpenAI Configuration
# ==============================================
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=4000

# ==============================================
# File Upload
# ==============================================
MAX_UPLOAD_SIZE=10485760
ALLOWED_EXTENSIONS=.pdf,.docx,.xlsx,.jpg,.png

# ==============================================
# Logging
# ==============================================
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# ==============================================
# JWT Settings
# ==============================================
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ==============================================
# Production Values (For reference only)
# ==============================================
# In production (AWS App Runner/ECS), set these via AWS Console:
#
# ENVIRONMENT=production
# DEBUG=false
# POSTGRES_SERVER=your-rds-endpoint.us-east-1.rds.amazonaws.com
# REDIS_HOST=your-elasticache-endpoint.cache.amazonaws.com
# USE_LOCAL_STORAGE=false
# S3_BUCKET=h2o-allegiant-storage  (or AWS_S3_BUCKET - both supported)
# S3_REGION=us-east-1
# CORS_ORIGINS=https://main.dxxxxx.amplifyapp.com  (update with real Amplify URL)
# BACKEND_URL=https://xxxxx.us-east-1.awsapprunner.com  (update with real App Runner URL)
#
# Secrets (via AWS Secrets Manager):
# OPENAI_API_KEY=<from Secrets Manager>
# SECRET_KEY=<from Secrets Manager>
# POSTGRES_PASSWORD=<from Secrets Manager>
#
# ⚠️ CRITICAL: Run database migrations on first deploy:
#   docker exec -it backend alembic upgrade head
